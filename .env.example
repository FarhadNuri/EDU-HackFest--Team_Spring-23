# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/harvestguard?retryWrites=true&w=majority

# JWT Secret (Generate a secure random string)
JWT_SECRET=your_super_secret_jwt_key_here_generate_random_string

# OpenWeather API Key (Get from https://openweathermap.org/api)
OPENWEATHER_API_KEY=your_openweather_api_key_here

# Upstash Redis URL (Get from https://upstash.com/)
UPSTASH_REDIS_URL=redis://:password@redis-endpoint.upstash.io:port

# Server Configuration
PORT=5000
NODE_ENV=production
