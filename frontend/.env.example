# Frontend Environment Variables for Production
# Copy this to .env.production and fill in your values

# Backend API URL (for production deployment)
# If backend and frontend are on same domain, you can use relative URLs
VITE_API_URL=/api

# Or if backend is on different domain:
# VITE_API_URL=https://your-backend-url.onrender.com/api
